/**
 * ⚠️ LEGACY IMPLEMENTATION — NOT USED BY THE RUNNING REACT + FASTAPI APPLICATION.
 * THE ACTIVE BACKEND IS `backend/`.
 *
 * These modules are the original Node.js end-to-end spike. They are frozen:
 * not integrated, not extended, and not deleted. Their tests still run via
 * `npm test` at the repository root, but that says nothing about the active
 * application. Do not add features here; make changes in `backend/`.
 */
