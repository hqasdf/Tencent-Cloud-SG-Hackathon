// adapters/gemini.js — Gemini CLI BeforeAgent adapter.
//
// P2 placeholder. Real implementation deferred until Gemini CLI hook
// support is verified on real hardware (rollout §5 Q5).

export function parse() { return null; }
