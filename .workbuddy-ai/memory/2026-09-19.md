- Built the RydeResolve frontend-first React + TypeScript prototype in frontend/. It uses typed API-shaped contracts, separate mock data and service layers, and a professional evidence-to-decision case UI with four mock cases, including a human-review escalation state.
- Validation: frontend dependencies installed; npm run build passes with TypeScript and Vite. Dev server runs at http://127.0.0.1:5173.
- Added backend/ FastAPI milestone: camelCase Pydantic responses matching frontend contracts, MockCaseRepository -> CaseService -> API separation, deterministic CaseReplay ordering/reference validation, and evidence-reference validation. Frontend case service now fetches localhost:8000 and has loading/API failure/not-found states; frontend mocks were removed. Validation: 11 pytest tests pass and the frontend production build passes.
- Completed Milestone 3 deterministic analysis: structured route/no-show source inputs drive route metrics, Haversine no-show checks, centralized prototype policy evaluation, calculated recommendations/refunds, confidence, and escalation. Added /api/cases/{case_id}/analysis plus a frontend calculated-analysis panel. Validation: 22 pytest tests pass; frontend production build passes; all four expected case outcomes were confirmed through API and browser checks with no application console errors.
- Revalidated the legacy root Node pipeline and fixed its human-review gate: non-auto-resolved disputes now return PENDING_HUMAN_REVIEW, zero refund, and ESCALATE_TO_HUMAN_REVIEW instead of leaking an executable judge ruling. Final validation: root npm test 10/10 passing; backend pytest 22/22 passing; frontend production build passing.
- Added run tooling for the project root: start-backend.bat, start-frontend.bat, and RUNNING.md (startup commands, URLs, first-time setup, test commands, and the Vite "port 5173 in use" explanation). Backend CORS now also allows ports 5174 so Vite port auto-increment does not break the frontend. RUNNING.md also documents the `python -m uvicorn` "No module named uvicorn" trap: use the venv interpreter at binaries/python/envs/default/Scripts/python.exe with PYTHONPATH=.
- Milestone 4 backend (Rider + Driver Advocate AI agents) implemented. Architecture rule: CODE calculates facts, AI argues from facts, CODE checks AI claims, Judge decides later. New packages: app/agents/ (models, config, provider protocol, providers/mock + providers/openai_compatible, context_builder, claim_verification, base_advocate, rider_advocate, driver_advocate, prompts/) and app/services/advocate_orchestrator.py. New endpoint POST /api/cases/{case_id}/advocates/run.
- Stage 4 safety boundaries that must not be weakened: (1) AgentCaseContext is built by explicit allow-list projection and never carries resolutionRecommendation, refundAmount, confidence, resolutionMode, escalationReasons, or the raw analysis_input; (2) AdvocateClaimVerificationService is separate from EvidenceValidationService (case-record validation vs AI-claim validation); (3) assertedFacts are hard-rejected on contradiction with a 1e-6 tolerance, prose contradictions are warnings only; (4) rejected claims are never repaired or dropped, they stay visible for audit; (5) advocates run independently with no cross-examination, and one side failing yields status FAILED without affecting the other or the deterministic analysis; (6) real mode fails loudly with AgentConfigurationError and never silently falls back to mock; (7) JUDGE is reported as NOT_RUN.
- Note on vocabulary overlap: ContextPolicyEvaluation.overall_outcome legitimately carries PolicyTwin outcome strings such as UPHOLD_CANCELLATION_CHARGE, which can coincidentally equal the resolution recommendation's action. That is required to be visible, so leak tests must assert on structured fields/keys rather than broad substring scans over the serialized context.
- Validation: backend pytest 132/132 passing (was 22 before Stage 4). New test files: test_advocate_context.py (11), test_claim_verification.py (19), test_advocate_output.py (8), test_provider_mock.py (19), test_advocate_agents.py (19), test_advocate_orchestrator.py (20), test_advocate_api.py (14, includes the mandatory end-to-end E_DOES_NOT_EXIST hallucination rejection test).
- Milestone 4 frontend: types/advocate.ts, caseService.runAdvocates(id) via POST, AdvocateRun.tsx (pipeline strip + live advocate panels), App.tsx run state, CaseDetail.tsx rewired. The old static riderCase/driverCase panels were REMOVED from the UI (the fields remain in the payload but are marked @deprecated with a comment warning against reintroducing a second hardcoded panel). Additive CSS appended at the end of styles.css; the existing minified rules were left untouched.
- Frontend bug found and fixed during browser validation: warning list keys were `${claimId}-${code}`, which collides when one claim raises several warnings of the same code (e.g. disputing two pieces of evidence), producing React duplicate-key console errors. Key now includes the array index.
- CORS: main.py now derives DEV_ORIGINS for ports 5173-5180 on both localhost and 127.0.0.1, because Vite auto-increments its port when 5173 is busy and the old fixed 5173/5174 list broke the app.
- Docs: root README.md created with the prominent legacy-pipeline warning; src/LEGACY.md plus banner comments in src/server.js, src/pipeline.js, src/advocates.js, src/judge.js; backend/.env.example; backend/README.md rewritten with endpoints, error behaviour, env vars, and the 10 Stage 4 safety properties.
- Final Milestone 4 validation: backend pytest 132/132; legacy npm test 10/10; frontend production build passes; browser run via Playwright + system Chrome confirmed 0 console errors and 0 page errors, JUDGE rendered as NOT RUN, live panels showing 5 verified claims on CASE-2026-1041, advocate panel correctly resets to empty on case switch (no stale mixing), and a stubbed E_DOES_NOT_EXIST claim rendered as REJECTED BY VERIFIER with its reason and fabricated ID visible. All four case /api/cases/{id}/analysis responses were byte-identical (md5) before and after running advocates, confirming the deterministic engine is untouched.
- Environment note for future browser validation: `agent-browser` is not installed. Use Playwright from the isolated workspace at binaries/node/workspace/node_modules with `chromium.launch({ channel: "chrome" })` to reuse the system Chrome (the Playwright-downloaded browser build is missing). Also set no_proxy=127.0.0.1,localhost and pass `--noproxy '*'` to curl, because this machine has HTTP_PROXY/HTTPS_PROXY env vars set that otherwise return 502 for localhost requests.
